import { Form, Head, Link, usePage } from '@inertiajs/react';
import { ArrowRight, CheckCircle2, ClipboardCheck, ShieldCheck } from 'lucide-react';
import InputError from '@/components/input-error';
import PasswordInput from '@/components/password-input';
import SiteHeader from '@/components/site-header';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Spinner } from '@/components/ui/spinner';
import { register } from '@/routes';
import { store } from '@/routes/login';
import { request } from '@/routes/password';
import type { Auth } from '@/types/auth';

type Props = {
    status?: string;
    canResetPassword: boolean;
    canRegister: boolean;
};

export default function Login({
    status,
    canResetPassword,
    canRegister,
}: Props) {
    const { auth } = usePage<{ auth: Auth }>().props;

    return (
        <>
            <Head title="Log in to TestMaker" />
            <div className="min-h-screen overflow-x-hidden bg-[#f8fafc] text-slate-900">
                <SiteHeader auth={auth} />
                <main className="pt-[74px]">
                    <section className="relative overflow-hidden px-4 py-10 sm:px-6 lg:px-8 lg:py-16">
                        <div className="pointer-events-none absolute -left-32 top-24 h-72 w-72 rounded-full bg-blue-100/80 blur-3xl" />
                        <div className="pointer-events-none absolute -right-28 bottom-0 h-80 w-80 rounded-full bg-indigo-100/70 blur-3xl" />
                        <div className="relative mx-auto grid max-w-[1080px] items-stretch gap-8 lg:grid-cols-[1fr_0.85fr] lg:gap-12">
                            <div className="flex flex-col justify-center px-1 py-4 sm:px-5 lg:py-12">
                                <p className="text-xs font-bold uppercase tracking-[0.18em] text-brand-700">Welcome back</p>
                                <h1 className="mt-4 max-w-xl font-display text-4xl font-extrabold leading-[1.08] tracking-[-0.06em] text-slate-900 sm:text-5xl">Your next great assessment starts here.</h1>
                                <p className="mt-5 max-w-lg text-base leading-7 text-slate-600">Sign in to create polished test papers, manage your school workspace, and keep every assessment organized.</p>
                                <div className="mt-8 grid gap-4 text-sm text-slate-600">
                                    <div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-emerald-50 text-emerald-600"><CheckCircle2 size={18} /></span><span>Create and customize papers in minutes</span></div>
                                    <div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-blue-50 text-brand-600"><ClipboardCheck size={18} /></span><span>Keep your subjects, questions, and templates together</span></div>
                                    <div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-violet-50 text-violet-600"><ShieldCheck size={18} /></span><span>Built for dependable school workflows</span></div>
                                </div>
                            </div>

                            <div className="rounded-[24px] border border-slate-200 bg-white p-5 shadow-[0_18px_48px_rgba(15,23,42,0.08)] sm:p-8">
                                <div className="mb-7 border-b border-slate-200 pb-5">
                                    <h2 className="font-display text-2xl font-bold tracking-[-0.04em] text-slate-900">Log in to your account</h2>
                                    <p className="mt-2 text-sm leading-6 text-slate-500">Use your account details to continue to TestMaker.</p>
                                </div>

                                {status && <div className="mb-5 rounded-[10px] border border-emerald-200 bg-emerald-50 px-3 py-2.5 text-sm font-medium text-emerald-700">{status}</div>}

                                <Form {...store.form()} resetOnSuccess={['password']} disableWhileProcessing autoComplete="off" className="grid gap-5">
                                    {({ processing, errors }) => (
                                        <>
                                            <div className="grid gap-2">
                                                <Label htmlFor="email">Email address</Label>
                                                <Input id="email" type="email" name="email" required autoFocus tabIndex={1} autoComplete="off" placeholder="you@school.com" className="h-12 rounded-[10px] border-slate-300 bg-white" />
                                                <InputError message={errors.email} />
                                            </div>

                                            <div className="grid gap-2">
                                                <div className="flex items-center justify-between gap-3">
                                                    <Label htmlFor="password">Password</Label>
                                                    {canResetPassword && <Link href={request()} className="text-sm font-semibold text-brand-700 transition hover:text-brand-900" tabIndex={5}>Forgot password?</Link>}
                                                </div>
                                                <PasswordInput id="password" name="password" required tabIndex={2} autoComplete="current-password" placeholder="Enter your password" className="h-12 rounded-[10px] border-slate-300 bg-white" />
                                                <InputError message={errors.password} />
                                            </div>

                                            <label className="flex items-center gap-3 text-sm text-slate-600">
                                                <input id="remember" type="checkbox" name="remember" tabIndex={3} style={{ accentColor: '#2563eb' }} className="h-4 w-4 rounded border border-slate-300 bg-white" />
                                                <span>Remember me</span>
                                            </label>

                                            <Button type="submit" className="mt-1 h-12 w-full rounded-[10px] bg-brand-600 text-sm font-bold text-white hover:bg-brand-700" tabIndex={4} disabled={processing} data-test="login-button">
                                                {processing && <Spinner />}
                                                Log in <ArrowRight size={16} />
                                            </Button>
                                        </>
                                    )}
                                </Form>

                                {canRegister && <p className="mt-6 text-center text-sm text-slate-500">New to TestMaker? <Link href={register()} className="font-bold text-brand-700 transition hover:text-brand-900" tabIndex={5}>Start your free trial</Link></p>}
                            </div>
                        </div>
                    </section>
                </main>
                <footer className="bg-[#0b2035] px-5 py-8 text-white sm:px-8">
                    <div className="mx-auto flex max-w-[1240px] flex-col gap-5 sm:flex-row sm:items-center sm:justify-between">
                        <Link href="/" className="flex items-center gap-2.5"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-white text-[#102b46]"><ClipboardCheck size={19} /></span><span className="font-display text-xl font-extrabold tracking-[-0.04em]">TestMaker</span></Link>
                        <div className="flex flex-wrap gap-x-5 gap-y-2 text-sm text-blue-200"><Link href="/" className="transition hover:text-white">Home</Link><Link href="/pricing" className="transition hover:text-white">Pricing</Link><span className="flex items-center gap-1"><CheckCircle2 size={15} className="text-emerald-300" /> Secure school workspace</span></div>
                    </div>
                </footer>
            </div>
        </>
    );
}

Login.layout = null;
