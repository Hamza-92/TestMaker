import { Form, Head, Link, usePage } from '@inertiajs/react';
import { ArrowRight, Check, CheckCircle2, ShieldCheck, Sparkles } from 'lucide-react';
import InputError from '@/components/input-error';
import PasswordInput from '@/components/password-input';
import SiteHeader from '@/components/site-header';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Spinner } from '@/components/ui/spinner';
import { login } from '@/routes';
import { store } from '@/routes/register';
import type { Auth } from '@/types/auth';

type RegisterPageProps = { auth: Auth };

const benefits = [
    'Create papers in minutes',
    'Organize questions by class and subject',
    'Download, print, and share with confidence',
];

export default function Register() {
    const { auth } = usePage<RegisterPageProps>().props;

    return (
        <>
            <Head title="Create your account" />
            <div className="min-h-screen overflow-x-hidden bg-white text-slate-900">
                <SiteHeader auth={auth} />
                <main className="pt-[74px]">
                    <section className="relative overflow-hidden bg-[linear-gradient(180deg,#EFF6FF_0%,#FFFFFF_78%)] px-4 py-12 sm:px-6 lg:px-8 lg:py-20">
                        <div className="pointer-events-none absolute -left-28 top-24 h-80 w-80 rounded-full bg-blue-100/70 blur-3xl" />
                        <div className="pointer-events-none absolute right-0 top-0 h-36 w-36 opacity-60 [background-image:radial-gradient(#93c5fd_1.5px,transparent_1.5px)] [background-size:13px_13px]" />
                        <div className="relative mx-auto grid max-w-[1120px] items-center gap-10 lg:grid-cols-[0.9fr_1.1fr] lg:gap-20">
                            <div className="max-w-xl">
                                <p className="text-xs font-bold uppercase tracking-[0.18em] text-brand-700">Start your free trial</p>
                                <h1 className="mt-4 font-display text-[38px] font-extrabold leading-[1.08] tracking-[-0.06em] text-slate-900 sm:text-[52px]">Make your next assessment easier to build.</h1>
                                <p className="mt-5 max-w-lg text-base leading-7 text-slate-600 sm:text-lg">Create your TestMaker account and bring your paper-building workflow into one calmer, more dependable workspace.</p>
                                <div className="mt-8 grid gap-4">
                                    {benefits.map((benefit) => <div key={benefit} className="flex items-center gap-3 text-sm font-medium text-slate-700"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-emerald-50 text-emerald-600"><Check size={16} /></span>{benefit}</div>)}
                                </div>
                                <div className="mt-9 flex items-center gap-3 text-sm text-slate-500"><ShieldCheck size={18} className="text-brand-600" /> Your account is protected with secure sign-in.</div>
                            </div>

                            <div className="rounded-[26px] border border-slate-200 bg-white p-6 shadow-[0_24px_60px_rgba(15,23,42,0.1)] sm:p-9">
                                <div className="mb-7 flex items-start gap-3 border-b border-slate-100 pb-6"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-brand-50 text-brand-600"><Sparkles size={21} /></span><div><h2 className="font-display text-2xl font-bold tracking-[-0.04em] text-slate-900">Create your account</h2><p className="mt-1 text-sm text-slate-500">Your school trial starts right away.</p></div></div>
                                <Form {...store.form()} resetOnSuccess={['password', 'password_confirmation']} disableWhileProcessing className="grid gap-5">
                                    {({ processing, errors }) => <>
                                <div className="border-b border-slate-100 pb-6">
                                    <p className="text-xs font-bold uppercase tracking-[0.14em] text-brand-700">School details</p>
                                    <div className="mt-4 grid gap-4 sm:grid-cols-2">
                                        <div className="grid gap-2 sm:col-span-2"><Label htmlFor="school_name">School name</Label><Input id="school_name" type="text" required autoComplete="organization" name="school_name" placeholder="Your school or institution" className="h-11 rounded-[10px]" /><InputError message={errors.school_name} /></div>
                                        <div className="grid gap-2"><Label htmlFor="phone">School contact number</Label><Input id="phone" type="tel" required autoComplete="tel" name="phone" placeholder="03xx xxxxxxx" className="h-11 rounded-[10px]" /><InputError message={errors.phone} /></div>
                                        <div className="grid gap-2"><Label htmlFor="city">City <span className="font-normal text-slate-400">(optional)</span></Label><Input id="city" type="text" autoComplete="address-level2" name="city" placeholder="City" className="h-11 rounded-[10px]" /><InputError message={errors.city} /></div>
                                        <div className="grid gap-2"><Label htmlFor="province">Province <span className="font-normal text-slate-400">(optional)</span></Label><Input id="province" type="text" autoComplete="address-level1" name="province" placeholder="Province" className="h-11 rounded-[10px]" /><InputError message={errors.province} /></div>
                                        <div className="grid gap-2 sm:col-span-2"><Label htmlFor="address">School address <span className="font-normal text-slate-400">(optional)</span></Label><Input id="address" type="text" autoComplete="street-address" name="address" placeholder="Street address" className="h-11 rounded-[10px]" /><InputError message={errors.address} /></div>
                                    </div>
                                </div>
                                <div className="border-b border-slate-100 pb-1"><p className="text-xs font-bold uppercase tracking-[0.14em] text-brand-700">Account owner</p></div>                                        <div className="grid gap-2"><Label htmlFor="name">Full name</Label><Input id="name" type="text" required autoFocus autoComplete="name" name="name" placeholder="Your full name" className="h-11 rounded-[10px]" /><InputError message={errors.name} /></div>
                                        <div className="grid gap-2"><Label htmlFor="email">Email address</Label><Input id="email" type="email" required autoComplete="email" name="email" placeholder="you@school.com" className="h-11 rounded-[10px]" /><InputError message={errors.email} /></div>
                                        <div className="grid gap-2"><Label htmlFor="password">Password</Label><PasswordInput id="password" required autoComplete="new-password" name="password" placeholder="At least 8 characters" className="h-11 rounded-[10px]" /><InputError message={errors.password} /></div>
                                        <div className="grid gap-2"><Label htmlFor="password_confirmation">Confirm password</Label><PasswordInput id="password_confirmation" required autoComplete="new-password" name="password_confirmation" placeholder="Repeat your password" className="h-11 rounded-[10px]" /><InputError message={errors.password_confirmation} /></div>
                                        <label className="flex items-start gap-3 text-xs leading-5 text-slate-500"><input type="checkbox" name="terms" required className="mt-1 h-4 w-4 rounded border-slate-300 accent-blue-600" /> <span>I agree to use TestMaker responsibly and understand that the trial can be ended at any time.</span></label>
                                        <Button type="submit" className="mt-1 h-12 w-full rounded-[10px] bg-brand-600 text-sm font-bold hover:bg-brand-700" data-test="register-user-button">{processing && <Spinner />} Create account <ArrowRight size={16} /></Button>
                                    </>}
                                </Form>
                                <p className="mt-6 text-center text-sm text-slate-500">Already have an account? <Link href={login()} className="font-bold text-brand-700 hover:text-brand-900">Log in</Link></p>
                            </div>
                        </div>
                    </section>
                    <section className="border-t border-slate-100 bg-white px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto flex max-w-[1120px] flex-col items-center justify-between gap-3 text-center text-xs text-slate-500 sm:flex-row sm:text-left"><div className="flex items-center gap-2"><CheckCircle2 size={16} className="text-emerald-500" /> No credit card required to start.</div><Link href="/pricing" className="font-bold text-brand-700 hover:text-brand-900">View pricing <ArrowRight size={14} className="ml-1 inline" /></Link></div></section>
                </main>
            </div>
        </>
    );
}

Register.layout = null;
